#!/bin/bash

# 1. go to https://app.localstack.cloud/quickstart-cloudformation
# 2. deploy the sample app (defined as CloudFormation stack)
# 3. send a request directly from the browser, then observe status changes in the table view
